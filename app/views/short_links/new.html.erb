<h1>Créer un lien</h1>
    <div class="container">
        <%= simple_form_for(@short_link) do |f| %>
        <div>
        <div class="form-group">
            <%= f.input :long_url %>
        </div>
         <div class="form-group">
            <%= f.input :description %>
        </div>
        <% if current_user %>
            <div class="form-group">
                <%= f.association :attraction, collection: @attractions %>
            </div>
        <% end %>
            <%= f.submit class: "btn btn-success" %>
        <div>
    <% end %>
</div>